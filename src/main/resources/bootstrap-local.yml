spring:
  application:
    name: order
  cloud:
    config:
      uri: http://localhost:8888
      label: feign

logging:
  level:
    com.netflix.hystrix: debug
    org.apache.http.impl: warn
    com.jpetstore.order: debug
    feign: debug
