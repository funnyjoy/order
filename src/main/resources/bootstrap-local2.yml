spring:
  application:
    name: order
  cloud:
    config:
      uri: http://localhost:8888

logging:
  level:
    com.netflix.hystrix: debug
    org.apache.http.impl: debug
    com.jpetstore.order: debug
    feign: debug
